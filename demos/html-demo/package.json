{
  "name": "html-demo",
  "type": "module",
  "version": "1.0.0",
  "scripts": {
    "build": "tsc && esbuild demo.ts --bundle --outdir=build"
  },
  "dependencies": {
    "jsondiffpatch": "^0.6.0"
  },
  "devDependencies": {
    "esbuild": "^0.19.8",
    "typescript": "~5.3.2"
  }
}
